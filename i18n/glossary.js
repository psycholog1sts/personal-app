export const localizationGlossary = Object.freeze([
  Object.freeze({ term: 'Row Level Security', short: 'RLS', policy: 'Translate the concept naturally, preserve the RLS initialism.' }),
  Object.freeze({ term: 'tenant isolation', policy: 'Translate consistently as an authorization-boundary concept.' }),
  Object.freeze({ term: 'authorization', policy: 'Use the locale-standard software-security term, not authentication.' }),
  Object.freeze({ term: 'release gate', policy: 'Translate consistently as a deterministic release-control step.' }),
  Object.freeze({ term: 'policy', policy: 'Translate in the database/security-policy sense.' }),
  Object.freeze({ term: 'finding', policy: 'Translate as a security/engineering finding, not a generic discovery.' }),
  Object.freeze({ term: 'verification', policy: 'Translate as technical verification.' }),
  Object.freeze({ term: 'coverage', policy: 'Translate as executed-check coverage.' }),
  Object.freeze({ term: 're-test', policy: 'Translate consistently as repeating the same verification after remediation.' }),
  Object.freeze({ term: 'Supabase', policy: 'Protected product name; do not translate.' }),
  Object.freeze({ term: 'PostgreSQL', policy: 'Protected product name; do not translate.' }),
  Object.freeze({ term: 'GitHub Actions', policy: 'Protected product name; do not translate.' }),
]);
